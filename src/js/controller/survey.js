angular.module("ctApp")
    .controller("surveyEdit", [
        "$scope",
        "$state",
        "$apis",
        "$timeout",
        function($scope, $state, $apis, $timeout) {


        }
    ]);